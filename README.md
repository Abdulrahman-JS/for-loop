*A simple and efficient Java program that generates a square-shaped pattern of stars * based on a user-provided integer input*.
🚀 How it Works
The program calculates the total number of stars required by squaring the input n (Total stars = n \times n). It then iterates through a single loop and uses the modulo operator (%) to determine when to move to a new line, creating a perfect square grid.
